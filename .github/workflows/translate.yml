name: Groq Fast Translation Runner

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  translate:
    runs-on: ubuntu-latest
    timeout-minutes: 350

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: pip install requests

      - name: Run Translation Script
        env:
          GROQ_API_KEYS: ${{ secrets.GROQ_API_KEYS }}
        run: python translate.py

      # 🚀 YEH STEP HAR HAAL MEIN CHALEGA AUR PROGRESS SAVE KAREGA
      - name: Commit and Push Checkpoint (Resume Data)
        if: always()
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add american_roman.oxt translation_checkpoint.json || true
          git commit -m "Auto-checkpoint: Saved translation progress" || echo "No changes to commit"
          git push || echo "Push skipped or failed"
